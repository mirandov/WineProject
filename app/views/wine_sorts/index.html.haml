%h1= t('app.wine_sorts.index')

%table.table.table-hover.table-striped.center
  %caption
    .pull-right
      = link_to t('app.wine_sorts.new-button'), new_wine_sort_path, class: 'btn btn-primary'
  %thead
    %tr
      %th= WineSort.human_attribute_name(:name)
      %th= WineSort.human_attribute_name(:type_of_wine)
      %th= WineSort.human_attribute_name(:color)
      %th= WineSort.human_attribute_name(:barrel_extract)
      %th= WineSort.human_attribute_name(:bottle_extract)
      %th.p50
      %th.p50
      %th.p50

  %tbody
    - @wine_sorts.each do |wine_sort|
      %tr
        %td= wine_sort.name
        %td= wine_sort.type_of_wine
        %td= wine_sort.color
        %td= wine_sort.barrel_extract
        %td= wine_sort.bottle_extract
        %td
          = link_to wine_sort, class: 'btn btn-success' do
            = fa_icon :eye
        %td
          = link_to edit_wine_sort_path(wine_sort), class: 'btn btn-info' do
            = fa_icon :edit
        %td
          = link_to wine_sort, :method => :delete, :data => { :confirm => t('helpers.sure') }, class: 'btn btn-danger' do
            = fa_icon :times
